# Analysis layer (offline, Python)

Pinned interpreter: Python `3.14.7`. No third-party dependencies yet —
`validate_logs.py` / `aggregate.py` arrive in M0-14 with minimal deps
(NumPy/Matplotlib only when actually needed). A notebook may explore results
but must never be the only executable record.
