# No third-party Python dependencies yet (M0).
# The log auditor uses only Python 3.14.7's standard library. Introduce a
# dependency lock alongside the first external dependency; none are resolved now.
